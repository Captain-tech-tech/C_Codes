/*#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int randomnum(int limit)
{
    srand(time(0));

    return rand() % limit;
}
void main()
{
    int limit;
    printf("Enter limit for your random number : ");
    scanf("%d",&limit);
    int a = randomnum(limit);

    printf("%d",a);
}*/
