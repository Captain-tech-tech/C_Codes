// 11 Write a program that uses a pointer to find the factorial of a number.
/*#include<stdio.h>
void fact(int *ptr) // int
{
    int num=*ptr;
    // int count = 0;
    *ptr=1;
    for(int i=num;i>=1;i--)
     {
        *ptr*=i;
        count ++;
     } 
        // return count;
}
int main()
{
    int n;printf("Enter a numer : ");scanf("%d",&n);

    printf("Your entered number is %d.\n",n);

    int *ptr=&n;
    fact(ptr);
    printf("The factorial of your number is %d.\n",n);
    return 0;
}*/
