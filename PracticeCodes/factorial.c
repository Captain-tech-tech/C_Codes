/*#include <stdio.h>
void main()
{
    int number,factorial = 1, i=1;
    again:
    printf("Enter positive number whose factorial you want to calculate : ");
    scanf("%d",&number);
    if(number < 0)
     {
        printf("Invalid input, negative numbers have no factorial.\n");
        goto again;
     }
    while( i<number)
       {
         factorial += factorial*i;
         i++;
       }

    printf("The factorial of %d is %d.",number,factorial);   

}*/
